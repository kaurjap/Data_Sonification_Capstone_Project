extends layout

block links
    script(src="/javascripts/search.js")
block content
    h3 Search a Stock Symbol
    form(id="searchForm")
        .form-row
            input#searchKeywords(type='text' name='searchKeywords' placeholder="Search..." autocomplete='false')
            button.block(type='submit' id="searchButton") Search
    .best-matches

