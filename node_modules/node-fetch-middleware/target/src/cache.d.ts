import { Middleware } from './middleware';
import { RequestInfo, RequestInit, Response } from 'node-fetch';
export declare type Key = (url: RequestInfo, init?: RequestInit) => string | null;
export declare type Ttl = (response: Response, url: RequestInfo, init?: RequestInit) => number | null;
export declare type Cache = {
    get(key: string): Promise<Response>;
    set(key: string, response: Response, ttl: number): Promise<void>;
};
export declare type CacheOptions = {
    key?: Key;
    ttl?: Ttl;
    cache?: Cache;
};
export declare function ttl(ttl: number): Ttl;
export declare function cache(options?: CacheOptions): Middleware;
