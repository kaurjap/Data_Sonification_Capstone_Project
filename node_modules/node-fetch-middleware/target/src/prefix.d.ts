import { Middleware } from './middleware';
export declare type PrefixOptions = {
    test?: (url: string) => boolean;
} | undefined;
export declare function prefix(prefix: string, { test }?: PrefixOptions): Middleware;
