import { Middleware } from './middleware';
declare module 'node-fetch' {
    interface RequestInit {
        query?: string | {
            [key: string]: string | string[];
        };
    }
}
export declare const query: Middleware;
